---
description: Writing typescript type testing
globs: **/*.test-d.?(c|m)ts?(x)
---
# Type Testing

## Recommended

+ Write type test with `vitest`: `expectTypeOf`, `describe`, `it`,...
+ Don't need `expect.assertions` at the beginning of each test.

## Custom
